"use client";
import { useState } from "react";
import { ArrowRight, CheckCircle2, Eye, EyeOff, LockKeyhole, Plane, ShieldCheck, UserRound } from "lucide-react";

export function AuthPage({ mode }: { mode: "login" | "signup" }) {
  const signup = mode === "signup"; const [showPassword, setShowPassword] = useState(false); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  const [form, setForm] = useState({ fullName: "", employeeId: "", email: "", role: "Flight Attendant", station: "ISB", identity: "", password: "", confirm: "" });
  async function submit(event: React.FormEvent) { event.preventDefault(); setError(""); if (signup && form.password !== form.confirm) return setError("Passwords do not match."); setSaving(true); const response = await fetch(`/api/auth/${mode}`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json(); setSaving(false); if (!response.ok) return setError(data.error ?? "Unable to continue."); const returnTo = new URLSearchParams(window.location.search).get("returnTo"); window.location.href = returnTo?.startsWith("/") ? returnTo : "/overview"; }
  function useDemo() { setForm({ ...form, identity: "sana.khan@airblue.com", password: "Crew@123" }); }
  return <main className="auth-page"><section className="auth-visual"><div className="auth-brand"><span className="brand-mark">a</span><strong>air<span>blue</span></strong><small>CREW OPERATIONS</small></div><div className="auth-visual-copy"><span>BLUECREW OPS</span><h1>Every flight ready.<br />Every handover clear.</h1><p>One accountable workspace for cabin readiness, service inventory and shift continuity.</p><div className="auth-points"><span><CheckCircle2 /> Verified pre-flight requisites</span><span><CheckCircle2 /> Live shortage visibility</span><span><CheckCircle2 /> Traceable crew handovers</span></div></div><div className="auth-flight-card"><Plane /><span><small>NEXT DEPARTURE</small><strong>PA201 · ISB → KHI</strong></span><b>11:20</b></div></section><section className="auth-form-side"><div className="auth-form-wrap"><div className="auth-form-icon">{signup ? <UserRound /> : <LockKeyhole />}</div><span className="auth-eyebrow">CABIN CREW ACCESS</span><h2>{signup ? "Create your crew account" : "Welcome back"}</h2><p>{signup ? "Register with your Airblue crew details." : "Sign in to continue to your assigned operations."}</p><form onSubmit={submit}>{signup && <><label>Full name<input required value={form.fullName} onChange={(e) => setForm({ ...form, fullName: e.target.value })} placeholder="Sana Khan" /></label><div className="auth-form-grid"><label>Employee ID<input required value={form.employeeId} onChange={(e) => setForm({ ...form, employeeId: e.target.value.toUpperCase() })} placeholder="AB-2047" /></label><label>Home station<input required value={form.station} onChange={(e) => setForm({ ...form, station: e.target.value.toUpperCase() })} placeholder="ISB" /></label></div><label>Airblue email<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="name@airblue.com" /></label><label>Crew role<select value={form.role} onChange={(e) => setForm({ ...form, role: e.target.value })}><option>Flight Attendant</option><option>Cabin Supervisor</option><option>Senior Purser</option></select></label></>}{!signup && <label>Email or employee ID<input required value={form.identity} onChange={(e) => setForm({ ...form, identity: e.target.value })} placeholder="name@airblue.com or AB-2047" /></label>}<label>Password<div className="password-field"><input required type={showPassword ? "text" : "password"} minLength={8} value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="Minimum 8 characters" /><button type="button" aria-label="Show password" onClick={() => setShowPassword(!showPassword)}>{showPassword ? <EyeOff /> : <Eye />}</button></div></label>{signup && <label>Confirm password<input required type="password" minLength={8} value={form.confirm} onChange={(e) => setForm({ ...form, confirm: e.target.value })} placeholder="Repeat your password" /></label>}{error && <div className="auth-error">{error}</div>}<button className="auth-submit" disabled={saving}>{saving ? "Please wait…" : signup ? "Create account" : "Sign in"}<ArrowRight /></button></form>{!signup && <div className="demo-account"><ShieldCheck /><span><strong>Demo cabin supervisor</strong>sana.khan@airblue.com · Crew@123</span><button onClick={useDemo}>Use demo</button></div>}<p className="auth-switch">{signup ? "Already registered?" : "New cabin crew member?"} <a href={signup ? "/login" : "/signup"}>{signup ? "Sign in" : "Create account"}</a></p><small className="auth-footnote">Authorized Airblue crew access only · Activity is recorded</small></div></section></main>;
}
